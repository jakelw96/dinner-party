const populateIntrests = () => {
    let foodIntrestArray = ['Camping', 'Fishing', 'Yoga', 'Liturature', 'Health', 'Dogs', 'Cats', 'Video Games', 'Skiing', 'Running', 'Meditation', 'Healing', 'Crossfit', 'Deep Conversations', 'Golf', 'Wine', 'Fashion', 'Gardening', 'Hiking', 'Cars', 'Coding', 'Kayaking', 'Baseball', 'Soccer', 'Motorcycles', 'Tools',];
    for(let i = 0; i < foodIntrestArray.length; i++) {
        //TODO: add button css with template 
        return ``
    }
}

const removeIntrests = (data) => {
    //TODO: add intrest to the user or group object user.intrests/group.intrests
    let intrestsArray = data.intrests

}

//looks at user or group object. tooks as the string data that it is passed and removes it from the intrest array
const addIntrest = (data) => {      
    let intrestsArray = data.intrests    
    //possible use the splice or filter methods then push back the remaining data to the user or group object.
}